@using System.Security.Claims;
@model Company_Detail_DTO

<div class="card mb-4">
	<div class="card-body">
		<div class="user-avatar-section pb-4 border-bottom">
			<div class=" d-flex align-items-center flex-column">
				<img class="img-fluid rounded mb-3 pt-1 mt-4" src="~/img/avatars/15.png" height="100" width="100" alt="User avatar">
				<div class="user-info text-center">
					<h4 class="mb-2">@Model.CompanyName</h4>
					<span class="badge bg-label-secondary mt-1">@@@Model.SysCompanyName?.ToString()</span>

				</div>
			</div>
		</div>
		<!--<div class="d-flex justify-content-around flex-wrap mt-3 pt-3 ">
			<div class="d-flex align-items-start me-4 mt-3 gap-2">
				<span class="badge bg-label-primary p-2 rounded"><i class="ti ti-checkbox ti-sm"></i></span>
				<div>
					<p class="mb-0 fw-semibold">1.23k</p>
					<small>@Localizer["Tasks Done"]</small>
				</div>
			</div>
			<div class="d-flex align-items-start mt-3 gap-2">
				<span class="badge bg-label-primary p-2 rounded"><i class="ti ti-briefcase ti-sm"></i></span>
				<div>
					<p class="mb-0 fw-semibold">568</p>
					<small>@Localizer["Projects Done"]</small>
				</div>
			</div>
		</div>-->
		<div class="mt-4 d-flex justify-content-between">
			<p class="small text-uppercase text-muted">@Localizer["Details"]</p>


		</div>



		<div class="info-container">
			<ul class="list-unstyled">
				@*  <li class="mb-2 pt-1">
                    <span class="fw-semibold me-1">@Localizer["Address"]:</span>
                    <span>@(Model.Address ?? "-/-") - @Model.AddressNum @Model.City, @Model.Country</span>
                </li>
                <li class="mb-2 pt-1">
                    <span class="fw-semibold me-1">@Localizer["E-Mail"]:</span>
                    <span>@Model.Email</span>
                </li>
                <li class="mb-2 pt-1">
                    <span class="fw-semibold me-1">@Localizer["Status"]:</span>
                    <span class="badge bg-label-success">Active</span>
                </li>

                <li class="mb-2 pt-1">
                    <span class="fw-semibold me-1">@Localizer["Phone"]:</span>
                    <span>@(Model.Phone ?? "-/-")</span>
                </li> *@
				<li class="mb-2 pt-1">
					<span class="fw-semibold me-1">@Localizer["Max Users"]:</span>
					<span>@Model.MaxUsers</span>
				</li>
				<li class="mb-2 pt-1">
					<span class="fw-semibold me-1">@Localizer["Language"]:</span>
					<span>-/-</span>
				</li>
				<li class="mb-2 pt-1">
					<span class="fw-semibold me-1">@Localizer["Timezone"]:</span>
					<span>@(Model.DefaultTimezoneID.HasValue ? "" : "-/-")</span>
				</li>
				<li class="mb-2 pt-1">
					<span class="fw-semibold me-1">@Localizer["Starting Contract"]:</span>
					<span>@Model.StartingContract.ToString("d")</span>
				</li>
				<li class="mb-2 pt-1">
					<span class="fw-semibold me-1">@Localizer["Ending Contract"]:</span>
					<span>@Model.EndingContract.ToString("d")</span>
				</li> 
				@*  <li class="text-center mb-2 pt-1">
                    @{
                        if (User.FindFirst(ClaimTypes.Role)?.Value == nameof(Core_Roles.Administrator) ||
                        User.FindFirst(ClaimTypes.Role)?.Value == nameof(Core_Roles.Employer) ||
                        User.FindFirst(nameof(Core_Const.COMP.COMP_EDIT))?.Value == true.ToString())
                        {
                            <a class="btn btn-warning" href="~/App/Company/Edit">
                                <i class="ti ti-edit me-2"></i>
                                <span class="d-none d-sm-block">@Localizer["Edit details"]</span>
                            </a>
                        }
                    }
                </li> *@
			</ul>
		</div>
	</div>
</div>